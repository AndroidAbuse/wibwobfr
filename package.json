{
  "name": "yokai-wibble-wobble-server",
  "version": "1.0.0",
  "description": "Private server for YO-KAI WATCH Wibble Wobble (EU)",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "gen-certs": "node scripts/gen-certs.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.0",
    "@supabase/supabase-js": "^2.45.0",
    "fastify": "^5.1.0",
    "uuid": "^10.0.0"
  },
  "devDependencies": {
    "selfsigned": "^2.4.1"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
